'use strict'

class User {
  constructor (fullName, email, description, signupDate) {
    this.fullName = fullName
    this.email = email
    this.description = description
    this.signupDate = signupDate
  }
}
